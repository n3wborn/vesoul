<div class="modal fade" id="modal-delete-order-{{ order.id }}" tabindex="-1" role="dialog" aria-labelledby="delete-modal" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title text-danger">Attention !</h2>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p class="h3 py-3 text-center">Veuillez confirmer votre choix</p>
            </div>
            <div class="modal-footer">
                <button class="btn btn-outline-danger" data-dismiss="modal">Annuler</button>
                <a class="btn btn-danger"
                   href="{{ path('dashboard_admin_delete_order', {'id' : order.id }) }}">
                    Supprimer
                </a>
            </div>
        </div>
    </div>
</div>

